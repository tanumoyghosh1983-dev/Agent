{
  "name": "case-study-agent",
  "version": "0.1.0",
  "private": true,
  "description": "An AI agent that turns raw text into a finished, designed case study — content, layout, images, and downloadable HTML — end to end with the OpenAI API.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
